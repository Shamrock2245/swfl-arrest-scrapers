# ============================================
# SWFL Arrest Scrapers — Python Dependencies
# ============================================
# Install: pip install -r python_scrapers/requirements.txt
# Mirrors pyproject.toml + additional scraper-specific deps
# ============================================

# --- Core (from pyproject.toml) ---
DrissionPage>=4.0
google-auth>=2.0
google-auth-oauthlib>=1.0
google-api-python-client>=2.0
gspread>=5.0
python-dotenv>=1.0
pyyaml>=6.0
requests>=2.28
beautifulsoup4>=4.12

# --- Browser Automation ---
selenium>=4.10
# playwright is installed separately via: playwright install chromium

# --- HTTP / Anti-Bot ---
curl_cffi>=0.6

# --- PDF Parsing ---
pdfplumber>=0.9

# --- Google Sheets Auth ---
oauth2client>=4.1.3
