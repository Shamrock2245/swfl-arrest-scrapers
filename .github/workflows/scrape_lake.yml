name: Scrape Lake County
on:
  schedule:
    - cron: '35 8,14,20,2 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: lake
      script: counties/lake/runner.py
      args: "--days-back 7"
      needs_xvfb: true
    secrets: inherit
