name: Scrape Glades County
on:
  schedule:
    - cron: '25 */6 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: glades
      script: counties/glades/runner.py
      args: "--days-back 7"
      needs_xvfb: false
    secrets: inherit
