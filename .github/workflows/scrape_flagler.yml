name: Scrape Flagler County
on:
  schedule:
    - cron: '50 */6 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: flagler
      script: counties/flagler/runner.py
      args: "--days-back 7"
      needs_xvfb: false
    secrets: inherit
