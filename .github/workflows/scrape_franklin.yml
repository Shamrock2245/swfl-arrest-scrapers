name: Scrape Franklin County
on:
  schedule:
    - cron: '20 */8 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: franklin
      script: counties/franklin/runner.py
      args: "--days-back 7"
      needs_xvfb: false
    secrets: inherit
