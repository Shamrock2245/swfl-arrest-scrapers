name: Scrape Sumter County
on:
  schedule:
    - cron: '50 */4 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: sumter
      script: counties/sumter/runner.py
      args: "--days-back 7"
      needs_xvfb: false
    secrets: inherit
