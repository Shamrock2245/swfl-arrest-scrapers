name: Scrape Bradford County
on:
  schedule:
    - cron: '5 */4 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: bradford
      script: counties/bradford/runner.py
      args: "--days-back 7"
      needs_xvfb: false
    secrets: inherit
