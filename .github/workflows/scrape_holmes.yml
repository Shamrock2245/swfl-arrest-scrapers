name: Scrape Holmes County
on:
  schedule:
    - cron: '40 */12 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: holmes
      script: counties/holmes/runner.py
      args: "--days-back 7"
      needs_xvfb: false
    secrets: inherit
