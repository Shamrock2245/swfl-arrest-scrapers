name: Scrape Escambia County
on:
  schedule:
    - cron: '25 */6 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: escambia
      script: counties/escambia/runner.py
      args: "--days-back 7"
      needs_xvfb: true
    secrets: inherit
