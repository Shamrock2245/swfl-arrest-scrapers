name: Scrape Nassau County
on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: nassau
      script: counties/nassau/runner.py
      args: "--days-back 7"
      needs_xvfb: false
    secrets: inherit
