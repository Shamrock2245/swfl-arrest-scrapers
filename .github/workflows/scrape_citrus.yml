name: Scrape Citrus County
on:
  schedule:
    - cron: '40 */6 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: citrus
      script: counties/citrus/runner.py
      args: "--days-back 7"
      needs_xvfb: false
    secrets: inherit
