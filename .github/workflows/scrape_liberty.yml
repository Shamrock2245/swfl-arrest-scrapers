name: Scrape Liberty County
on:
  schedule:
    - cron: '45 */12 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: liberty
      script: counties/liberty/runner.py
      args: "--days-back 7"
      needs_xvfb: false
    secrets: inherit
