name: Scrape Clay County
on:
  schedule:
    - cron: '15 */4 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: clay
      script: counties/clay/runner.py
      args: "--days-back 7"
      needs_xvfb: false
    secrets: inherit
