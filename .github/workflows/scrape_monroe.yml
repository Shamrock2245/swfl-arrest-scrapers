name: Scrape Monroe County
on:
  schedule:
    - cron: '35 */6 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: monroe
      script: counties/monroe/runner.py
      args: "--days-back 7"
      needs_xvfb: false
    secrets: inherit
