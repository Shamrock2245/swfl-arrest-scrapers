name: Scrape Martin County
on:
  schedule:
    - cron: '45 8,14,20,2 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: martin
      script: counties/martin/runner.py
      args: "--days-back 7"
      needs_xvfb: true
    secrets: inherit
