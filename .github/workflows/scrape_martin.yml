name: Scrape Martin County
on:
  schedule:
    - cron: '10 10,16,22,4 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: martin
      script: counties/martin/runner.py
      args: "--days-back 7"
      needs_xvfb: true
    secrets: inherit
