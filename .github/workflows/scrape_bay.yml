name: Scrape Bay County
on:
  schedule:
    - cron: '10 */4 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: bay
      script: counties/bay/runner.py
      args: "--days-back 7"
      needs_xvfb: false
    secrets: inherit
