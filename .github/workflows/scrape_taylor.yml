name: Scrape Taylor County
on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:
jobs:
  scrape:
    uses: ./.github/workflows/scrape.yml
    with:
      county: taylor
      script: counties/taylor/runner.py
      args: "--days-back 7"
      needs_xvfb: false
    secrets: inherit
